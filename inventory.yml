all:
  hosts:
    159.65.255.159:
      ansible_user: "{{ ansible_user }}"
      ansible_ssh_private_key_file: "{{ ansible_ssh_private_key_file }}"
      hostname: centos-lab
    161.35.134.20:
      ansible_user: "{{ ansible_user }}"
      ansible_ssh_private_key_file: "{{ ansible_ssh_private_key_file }}"
      hostname: ubuntu-lab
      

  children:
    ubuntu:
      hosts:
        161.35.134.20
    centos:
      hosts:
        159.65.255.159
